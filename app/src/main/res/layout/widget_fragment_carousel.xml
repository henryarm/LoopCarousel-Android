<?xml version="1.0" encoding="utf-8"?>
<com.armshare.loopcarousel.CarouselRelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/root_container"
    android:layout_width="match_parent"
    android:layout_height="400dp"
    android:background="@android:color/transparent"
    android:gravity="center"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/pagerImg"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:contentDescription="@string/app_name"
        android:elevation="10dp"
        android:src="@drawable/cat1"
        android:scaleType="centerCrop" />

    <RelativeLayout
        android:id="@+id/textLayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/shadow_rect"
        android:elevation="10dp"
        >

        <TextView
            android:id="@+id/label"
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:gravity="center_horizontal|bottom"
            android:layout_marginTop="@dimen/carousel_margin_top_title"
            android:paddingStart="@dimen/carousel_padding_title"
            android:paddingEnd="@dimen/carousel_padding_title"
            android:singleLine="true"
            android:text="Test"
            android:ellipsize="end"
            android:layout_centerInParent="true"
            android:textColor="@color/white"
            android:textSize="@dimen/text_size_text_small" />
        <TextView
            android:id="@+id/description"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_below="@id/label"
            android:layout_marginTop="@dimen/carousel_margin_top_desc"
            android:layout_centerHorizontal="true"
            android:gravity="center"
            android:layout_marginBottom="@dimen/carousel_description_text_margin_bottom"
            android:paddingStart="@dimen/carousel_padding_title"
            android:paddingEnd="@dimen/carousel_padding_title"
            android:lines="2"
            android:ellipsize="end"
            android:text="Description"
            android:textColor="@color/white"
            android:textSize="@dimen/text_size_text_smallest" />
    </RelativeLayout>


</com.armshare.loopcarousel.CarouselRelativeLayout>